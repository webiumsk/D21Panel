---
title: "Aqua: descriptor format and troubleshooting"
category: wallet-connection
order: 5
meta_description: "Descriptor format (e.g. ct(slip77(...),elsh(wpkh(...)))), typical errors. That the descriptor is watch-only. What to do if “descriptor already in use” (use another wallet/descriptor)."
---

Descriptor format (e.g. ct(slip77(...),elsh(wpkh(...)))), typical errors. That the descriptor is watch-only. What to do if “descriptor already in use” (use another wallet/descriptor).
